# Корпоративный RAG-чатбот на локальной LLM

Чат-бот, отвечающий сотрудникам на вопросы по внутренним файлам (прайс-листы,
презентации, записи обучения и т.д.). Полностью локальный — данные не покидают
сервер. Работает на двух платформах с общим кодом: **Apple Silicon** (Mac Studio,
Ollama/Metal) и **Linux + NVIDIA GPU** (vLLM/CUDA). Управление целиком через
веб-панель: режимы (вектор / Hybrid+ / LightRAG), дообучение (LoRA), смена модели,
загрузка файлов, аналитика и переустановка.

## Документация

- [docs/INSTALL.md](docs/INSTALL.md) — полная установка (Apple, GPU, деплой из GitHub, hybrid+, troubleshooting).
- [docs/USAGE.md](docs/USAGE.md) — использование: веб-панель, режимы, справочник настроек, API, операции.
- [docs/METHODOLOGY.md](docs/METHODOLOGY.md) — методология: RAG, конвейер, поиск/реранк, режимы, граф.
- [docs/MODELS.md](docs/MODELS.md) — описание моделей и рекомендации по выбору (LLM Ollama/vLLM, эмбеддер, база для дообучения).
- [finetune/README_finetune.md](finetune/README_finetune.md) — вариант с дообучением (LoRA) и переключение модели генерации.

## Деплой из GitHub на чистый сервер (GPU)

На чистом Linux-сервере с драйвером NVIDIA одной командой:

```bash
sudo -E REPO=https://github.com/USER/rag-chatbot.git ADMIN_TOKEN='пароль' bash deploy.sh
```

или, скачав скрипт прямо из репозитория:

```bash
curl -fsSL https://raw.githubusercontent.com/USER/rag-chatbot/main/deploy.sh \
  | sudo -E REPO=https://github.com/USER/rag-chatbot.git ADMIN_TOKEN='пароль' bash
```

`deploy.sh` клонирует репозиторий в `/opt/rag` и запускает полную GPU-установку
(Docker + NVIDIA toolkit, vLLM + Qdrant, Python-окружение, systemd-сервис
`rag-api`). После старта откройте `http://<ip>:8000` → раздел «Администратор» и
настройте папку с документами, модель и т.д. прямо в веб-панели.

Обновление до нового релиза (git pull + перезапуск):

```bash
sudo bash /opt/rag/update.sh            # с переиндексацией: sudo REINDEX=1 bash /opt/rag/update.sh
```

Для локального запуска на **Mac Studio** (Apple Silicon) деплой из GitHub не нужен —
см. раздел установки ниже (`setup.sh`).

## Как это устроено (архитектура)

Классический **RAG** (Retrieval-Augmented Generation): мы не дообучаем модель, а
на каждый вопрос находим релевантные куски документов и отдаём их LLM как контекст.
Это даёт точные ответы со ссылками на источники, мгновенное обновление при
изменении файлов и отсутствие «галлюцинаций» по ценам.

```
┌──────────────┐   индексация (ingest.py)        ┌───────────────┐
│  Папка с      │  парсинг → чанки → эмбеддинги    │   Qdrant       │
│  документами  │ ───────────────────────────────►│ (векторная БД) │
└──────────────┘  (PDF/DOCX/PPTX/XLSX/аудио)      └───────┬────────┘
                                                          │ поиск top-20
        вопрос                                            ▼
┌──────────────┐   /chat     ┌─────────────────┐   ┌───────────────┐
│  Веб-чат      │ ──────────► │  FastAPI (app)   │──►│ гибрид + реранк│
│ (браузер)     │ ◄────────── │  стриминг ответа │   │ (bge-reranker) │
└──────────────┘   ответ +   └────────┬─────────┘   └───────┬────────┘
                   источники           │ контекст + вопрос    │ top-6
                                       ▼                      │
                              ┌─────────────────┐ ◄───────────┘
                              │ Ollama + LLM     │
                              │ (Qwen3.6, Metal) │
                              └─────────────────┘
```

### Выбор компонентов и почему

**LLM — `qwen3.6:35b-a3b-q4_K_M` (MoE) через Ollama (по умолчанию).** MoE 35B с ~3B
активных параметров: быстрее плотной 32B при сопоставимом качестве и сильном русском,
~19–22 ГБ. При желании можно поднять `qwen2.5:72b` (~42 ГБ, качественнее, медленнее),
плотную `qwen3.6:27b` или специализированные русские модели — меняется одной строкой
в `.env` или выбором в админке. Полный справочник моделей и рекомендации —
[docs/MODELS.md](docs/MODELS.md). Ollama использует Metal-ускорение Apple «из
коробки», без возни с драйверами.

**Эмбеддинги — `BAAI/bge-m3`.** Многоязычная модель с сильным русским, размерность
1024, работает на MPS (Metal). Один из лучших открытых вариантов для смешанных
RU/EN корпусов.

**Реранкер — `BAAI/bge-reranker-v2-m3`.** Кросс-энкодер, который переоценивает
top-20 кандидатов и оставляет 6 действительно релевантных. Это главный рычаг
качества: именно реранк убирает «похожий, но не тот» контекст и даёт порог
`MIN_SCORE` для честного «не знаю».

**Векторная БД — Qdrant.** Быстрая, с фильтрами по метаданным и инкрементальным
обновлением. До тысяч файлов работает с запасом на одном сервере.

**Поиск — гибридный.** Плотный (семантический) поиск ловит смысл, BM25 —
точные совпадения (артикулы, названия товаров, числа в прайсах). Их комбинация
важна именно для прайс-листов.

### Особенности под ваши данные

- **Прайс-листы (XLSX/CSV)** разбираются построчно в формат `колонка: значение`,
  чтобы модель не путала цены между строками.
- **Презентации (PPTX)** индексируются по слайдам, включая заметки докладчика.
- **Записи обучения (аудио/видео)** автоматически транскрибируются Whisper
  (`mlx-whisper`, Metal) и индексируются как текст.
- **Сканы, фото и чертежи**: изображения (JPG/PNG/…) и RAW-фото (CR2/…) проходят
  OCR (Tesseract, rus+eng); чертежи DXF/DWG и 3D-CAD STEP/IGES — извлечение текста.
- **Письма, архивы и прочее**: Outlook `.msg`, старый `.doc`, HTML/MHTML, XML/JSON,
  SVG; архивы (ZIP/RAR/7Z/TAR) распаковываются, и содержимое индексируется.
- **Цитирование**: каждый ответ содержит источники (файл + страница/слайд).
- **Визуальная выдача**: рядом с источниками показываются миниатюры изображений,
  превью чертежей DXF/DWG, плеер аудио и кадр видео с перемоткой на нужный момент и
  вырезкой фрагмента по таймкодам (`media.py`, нужны `matplotlib`/`ffmpeg`).

## Установка на Apple Silicon (локально)

> Для Linux + NVIDIA и деплоя из GitHub см. [docs/INSTALL.md](docs/INSTALL.md)
> и раздел «Варианты деплоя» ниже.

```bash
# 1. Положить проект на сервер и запустить bootstrap
chmod +x setup.sh
./setup.sh            # Homebrew, Ollama, Qdrant, Python, скачивание моделей

# 2. Указать папку с документами
nano .env             # DOCS_DIR=/путь/к/документам

# 3. Проиндексировать
source .venv/bin/activate
python ingest.py

# 4. Запустить сервис
uvicorn app:app --host 0.0.0.0 --port 8000
```

Сотрудники открывают `http://<ip-сервера>:8000`.

## Эксплуатация

**Обновление индекса.** `python ingest.py` инкрементален — переиндексирует только
изменённые/новые файлы. Поставьте на расписание (см. `reindex.plist` — пример
launchd-задачи, запуск каждый час).

**Качество ответов** настраивается в `.env`: `TOP_K_RERANK` (объём контекста),
`MIN_SCORE` (строгость «не знаю»), `CHUNK_SIZE`. Промпт — в `prompts.py`.

**Доступ.** По умолчанию сервис слушает весь LAN. Для продакшена закройте за
reverse-proxy (Caddy/Nginx) с авторизацией или ограничьте firewall'ом.

## Структура

Ядро (общее для всех платформ):

| Файл                | Назначение                                        |
|---------------------|---------------------------------------------------|
| `app.py`            | FastAPI: чат + всё API (дашборд, журнал, аналитика, админ-операции) |
| `config.py`         | Чтение `.env` (стартовые значения)                |
| `settings.py`       | Рантайм-настройки и режимы (правятся из админки, `runtime_config.json`) |
| `loaders.py`        | Парсеры форматов (PDF/DOCX/DOC/PPTX/XLSX, изображения+OCR, CAD, .msg, архивы, …) + транскрибация |
| `media.py`          | Выдача артефактов: превью изображений/чертежей, кадры и фрагменты видео (с диска или из PostgreSQL) |
| `cache.py`          | Опциональный кэш Redis для тяжёлых агрегатов (по умолчанию выключен) |
| `fsutil.py`         | Устойчивый обход каталогов (пропуск недоступных папок, Errno 5 на сетевых шарах) |
| `backup.py`         | Резервные копии (настройки/служебные/полная) с SHA-256, проверка целостности и восстановление |
| `telegram_bot.py`   | Телеграм-бот (long-polling) с подтверждением пользователей; история Телеграм — отдельно |
| `metadata.py`       | Rule-based метаданные (категория, дата, тип)       |
| `enrich.py`         | LLM-метаданные при индексации (продукт/тема/тип)   |
| `ingest.py`         | Индексация в Qdrant (чанкинг, эмбеддинги, обработка ошибок, время обработки в `ingest_stats.json`) |
| `retriever.py`      | Гибридный поиск (dense + BM25) + реранк            |
| `query_filters.py`  | Умные фильтры из вопроса (LLM)                     |
| `graph_rag.py`      | Лёгкий граф-RAG (LightRAG на vLLM + bge-m3)        |
| `llm_backend.py`    | Единый интерфейс к LLM (Ollama / vLLM)             |
| `prompts.py`        | Системный промпт и сборка контекста                |
| `db.py`             | Журнал и каталог: запросы, оценки, тайминги, аналитика, история Телеграм; переключаемый бэкенд **SQLite / MySQL / PostgreSQL** (копирование/миграция, kv_store); каталог документов целиком в PostgreSQL (с SHA-256 и Large Objects) |
| `admin_ops.py`      | Операции админки: статус, реиндекс, граф, дообучение, модели, сброс, обзор папок, сайты, самотесты, бенчмарк, калибровка, операции с БД/Redis/каталогом в PostgreSQL |
| `remote.py`         | Удалённые хосты по SSH (деплой Qdrant/LightRAG, перенос данных, переключение) |
| `compare.py`        | Сравнение вектор-пайплайна и LightRAG              |
| `static/index.html` | Веб-панель (SPA) с темами оформления               |
| `docker-compose.yml`| Qdrant                                             |

Варианты и скрипты:

| Путь                       | Назначение                                          |
|----------------------------|-----------------------------------------------------|
| `setup.sh`                 | Bootstrap на Apple Silicon (Ollama + Qdrant + venv) |
| `deploy.sh` / `update.sh`  | Деплой/обновление из GitHub на GPU-сервер           |
| `reinstall.sh`             | Переустановка окружения (.venv + зависимости)       |
| `reinstall_server.sh`      | Полная переустановка GPU-сервера (destructive)      |
| `gpu_variant/`             | Linux + NVIDIA: vLLM, Qdrant, systemd, hybrid+, дообучение |
| `mac_variant/`             | Деплой/управление/переустановка на Mac (launchd)    |
| `windows_variant/`         | Windows: нативная установка (PowerShell) и Docker-вариант (`docker/`); папка документов по умолчанию `C:\db` |
| `docker_variant/`          | Кросс-платформенный Docker с Redis (Windows/Linux/macOS), авто-установка одной командой; GPU-override (`docker-compose.gpu.yml`) |
| `lightrag_variant/`        | Отдельная ветка LightRAG для сравнения              |
| `remote_variant/`          | Split-развёртывание: бэкенд (Qdrant+vLLM) + сервер приложения |
| `finetune/`                | Дообучение (QLoRA): датасет, обучение, README       |
| `docs/`                    | INSTALL, USAGE, METHODOLOGY, MODELS (+ .docx)       |

## Веб-панель

После запуска на `http://<сервер>:8000` доступна панель (с темами оформления —
Тёмная / Cyberpunk 2077 / Лето):

- **Дашборд** — счётчики: всего/сегодня, доля ответов, релевантность, задержка,
  среднее время поиска и генерации, число чанков.
- **Схема работы и тесты** — актуальная схема связки + тестирование каждого
  компонента с общим вердиктом и логом.
- **Чат** — стриминг ответа + источники; оценка 👍/👎; галка «подробно об источниках
  и алгоритме»; приложить документ (📎) к вопросу. Каждая сессия — отдельный чат
  (свой `session_id`); кнопки «🆕 Новый чат» (очистить и начать заново) и
  «💾 Сохранить историю» (выгрузить переписку в Markdown-файл). Поддержан
  **голосовой ввод** (🎤 → локальный Whisper) и **ответ голосом** (синтез речи браузера).
  Доступ к базе знаний есть и через **Телеграм-бота** (с подтверждением пользователей).
- **Журнал** — все запросы: вопрос, категория, релевантность, задержка, источники.
- **Аналитика** — по дням/категориям, топ источников, задержки, тайминги этапов,
  оценки ответов, частые слова.
- **Аналитика+** — графики по компонентам: Qdrant, граф, дообучение и тайминги
  (этапы, бенчмарк, длительности операций).
- **Система** — состояние и статистика Qdrant, LightRAG, дообучения, Hybrid+.
- **Телеграм** — статус бота, **запросы на доступ** (подтверждение пользователей
  вручную), список пользователей (подтвердить/заблокировать) и **отдельная история
  и запросы из Телеграм** со своей статистикой и очисткой.
- **Загрузка сервера** — текущая загрузка хоста в реальном времени: CPU (общая и по
  ядрам), память и swap, диски, GPU (NVIDIA — загрузка/видеопамять/температура/
  питание; Apple — единая память), сеть, аптайм, топ процессов; автообновление 2 с.
- **Бенчмарк** — замер скорости компонентов (эмбеддер, реранк, поиск, Qdrant,
  LLM ток/с) с возможностью остановки.
- **Администратор** — всё управление (под `ADMIN_TOKEN`):
  - режим (Базовый / С метаданными / Hybrid+), движок (Векторный / LightRAG),
    модель генерации (базовая / дообученная);
  - выбор/загрузка модели LLM из каталога (Qwen3.6/Qwen3/Gemma3/Llama и др.) с
    рекомендациями и **прогрессом загрузки** (проценты, объём, скорость);
    настройки на лету (TOP_K, порог, температура, промпт);
  - выбор папки документов, загрузка отдельных файлов и **целой папки** (до десятков
    тысяч файлов, батчами), добавление сайтов (до 50, парсинг), переиндексация,
    построение графа, дообучение;
  - переключатели тяжёлых экстракторов (OCR картинок/RAW, CAD, транскрибация) и лимит
    времени на файл — для ускорения индексации больших каталогов;
  - каталог документов: пагинация, сортировка, время обработки, признаки
    транскрипции/распознавания, превью и просмотр содержимого по каждому файлу;
  - **резервное копирование** (настройки / служебные данные / полная копия с
    каталогом) с контролем целостности SHA-256 и восстановлением с пред-проверкой;
  - установка компонентов (Qdrant/LightRAG), калибровка по оценкам ответов;
  - очистка истории всех чатов и статистики (журнал запросов + оценки);
  - удалённые хосты по SSH (деплой, перенос данных, переключение);
  - **база данных и кэш**: журнал/история/настройки можно хранить в **SQLite**
    (по умолчанию), **MySQL** или **PostgreSQL** — с проверкой подключения,
    копированием и миграцией между СУБД прямо из админки; опциональный кэш **Redis**
    для статистики/аналитики (по умолчанию выключен); расширенная статистика по каждой
    БД и Redis в разделе «Система»;
  - **каталог документов в PostgreSQL**: загрузка документов **целиком** (содержимое
    файла + имя + расширение + SHA-256; небольшие файлы — в bytea, крупные до **100 ГБ** —
    как Large Object потоково), переключение работы каталога на PostgreSQL и обратно,
    очистка только файлов. После этого можно **работать полностью без папки**: индексация,
    поиск, предпросмотр текста и медиа (изображения, PDF, чертежи, кадры/фрагменты видео,
    скачивание) идут из PostgreSQL; новые загрузки и спарсенные сайты также попадают в БД;
  - проверка обновлений и обновление; сброс данных и переустановка.

## Хранение данных: SQLite / MySQL / PostgreSQL и Redis

По умолчанию журнал запросов, история Телеграм и аналитика лежат в локальном **SQLite**
(`rag_logs.db`) — без внешних сервисов. При необходимости их можно перенести в **MySQL**
или **PostgreSQL** (раздел «Администратор» → «База данных и кэш»: заполнить подключение,
проверить, скопировать/мигрировать). Опционально включается кэш **Redis** для тяжёлых
агрегатов (сбрасывается при каждом новом запросе, поэтому данные не устаревают).

Отдельный сценарий — **хранить и сами документы в PostgreSQL**: загрузить файлы целиком в
таблицу `doc_catalog`, переключить источник каталога на PostgreSQL и переиндексировать
из БД. Дальше папка с файлами не нужна — всё (поиск, каталог, предпросмотр, медиа) работает
из PostgreSQL. Подробности и пошаговый сценарий — в [docs/USAGE.md](docs/USAGE.md).

## Варианты деплоя

- **Apple Silicon (Mac):** `setup.sh` локально, либо из GitHub —
  `mac_variant/deploy_mac.sh` (клон + установка + автозапуск через launchd).
- **Linux + NVIDIA (GPU):** `gpu_variant/run_gpu.sh` локально, либо из GitHub —
  `deploy.sh` (клон + vLLM/Qdrant + systemd). Генерация на vLLM (высокий throughput).
- **Windows:** `windows_variant/setup_windows.ps1` (PowerShell + winget + Ollama +
  Docker/Qdrant + Scheduled Task), либо `deploy_windows.ps1` из GitHub. Полностью
  контейнерный вариант — `windows_variant/docker/start_windows_docker.ps1`
  (приложение и Qdrant в Docker, генерация через Ollama на хосте). Папка документов
  по умолчанию — `C:\db`.
- **Docker с Redis (Windows/Linux/macOS):** `docker_variant/` — единый кросс-платформенный
  вариант, где Redis включён и настроен; авто-установка одной командой (`start.cmd` на
  Windows, `./start.sh` на Linux/macOS — ставят Docker и Ollama сами). GPU NVIDIA —
  через `-f docker-compose.gpu.yml`.
- **Обновление:** `update.sh` (GPU) / `mac_variant/update_mac.sh` (Mac).
- **Переустановка:** окружения — `reinstall.sh` или кнопка в админке; полная —
  `reinstall_server.sh` / `mac_variant/reinstall_mac.sh`.
- **Разнесённое (split):** Qdrant + vLLM на отдельном GPU-сервере, приложение —
  на другом. `remote_variant/setup_backend.sh` + `remote_variant/setup_app.sh`
  (см. [remote_variant/README_remote.md](remote_variant/README_remote.md)).

Код приложения общий; платформа определяется настройками (`LLM_BACKEND`, `DEVICE`),
которые задаются скриптами и правятся в админке.
