# Зависимости приложения для запуска в Docker (Linux, CPU).
# Отличия от requirements.txt: вместо mlx-whisper (только Apple) — faster-whisper;
# torch ставится отдельно в Dockerfile (CPU-сборка).

# --- веб/API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
httpx==0.28.1
socksio==1.0.0            # SOCKS5-прокси для httpx (Телеграм-бот за прокси)
python-multipart==0.0.18
paramiko==3.5.0

# --- векторная БД ---
qdrant-client==1.12.2

# --- эмбеддинги и реранк (CPU) ---
sentence-transformers==3.3.1
FlagEmbedding==1.3.3

# --- лексический поиск (гибрид) ---
rank-bm25==0.2.2

# --- парсинг документов ---
pymupdf==1.25.1            # PDF
python-docx==1.1.2         # DOCX
python-pptx==1.0.2         # PPTX
openpyxl==3.1.5            # XLSX/XLSM
xlrd==2.0.1                # старый XLS
ezdxf==1.3.5               # чертежи DXF/DWG
rawpy==0.24.0              # RAW-фото (CR2/NEF/ARW…) → OCR
Pillow==11.0.0             # растровые изображения для OCR/превью
matplotlib==3.10.0         # рендер чертежей DXF/DWG в превью
pytesseract==0.3.13        # OCR (нужен системный Tesseract)
extract-msg==0.54.1        # письма Outlook (.msg)
py7zr==0.22.0              # архивы .7z
rarfile==4.2               # архивы .rar (нужен системный unrar/7z)
pandas==2.2.3              # прайс-листы / таблицы
unstructured==0.16.11      # универсальный фолбэк
beautifulsoup4==4.12.3     # HTML/MHTML
markdown-it-py==3.0.0

# --- транскрибация (CPU/GPU, без Apple) ---
faster-whisper==1.1.0

# --- утилиты ---
tqdm==4.67.1
tenacity==9.0.0
psutil==6.1.1
