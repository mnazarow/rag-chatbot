"""Инфраструктурный пакет административной подсистемы RAG.

Содержит вынесенные из монолитного ``admin_ops.py`` общие компоненты:
  - ``common``  — чистые хелперы (форматирование, хэши, размеры) и наборы расширений;
  - ``jobs``    — примитивы фоновых задач (чтение логов, безопасная сериализация статуса).

``admin_ops`` ре-экспортирует эти имена, поэтому обратная совместимость
(``import admin_ops; admin_ops.<имя>``) сохраняется.
"""
