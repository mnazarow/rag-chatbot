"""Инфраструктурный пакет административной подсистемы RAG.

Содержит вынесенные из монолитного ``admin_ops.py`` общие компоненты и домены:
  - ``common``    — чистые хелперы (форматирование, хэши, размеры, классификация
                    файлов ``_file_method``, сводки ``_extract_summary``/``_ingest_stats``)
                    и наборы расширений;
  - ``jobs``      — примитивы фоновых задач (чтение логов, безопасная сериализация статуса);
  - ``web_crawl`` — полный домен веб-парсинга (``_Renderer``, ``_web_*`` fetch/download/
                    crawl/robots/sitemap/auth/excludes/cfg, ``ingest_web``, ``delete_web``,
                    ``web_structure``) со своим статусом задачи ``_web_job``.

``admin_ops`` ре-экспортирует эти имена, поэтому обратная совместимость
(``import admin_ops; admin_ops.<имя>``) сохраняется.
"""
