# Python-зависимости приложения для GPU-сервера (Linux).
# vLLM крутится в Docker-контейнере, тут — только клиент RAG + эмбеддинги/реранк на CUDA.
#
# ВАЖНО про torch: ставьте сборку под вашу CUDA, например:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124
# затем уже:  pip install -r requirements-gpu.txt

# --- веб/API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
httpx==0.28.1

# --- векторная БД ---
qdrant-client==1.12.2

# --- эмбеддинги и реранк (CUDA) ---
sentence-transformers==3.3.1
FlagEmbedding==1.3.3

# --- лексический поиск ---
rank-bm25==0.2.2

# --- парсинг документов ---
pymupdf==1.25.1
python-docx==1.1.2
python-pptx==1.0.2
openpyxl==3.1.5
pandas==2.2.3
unstructured==0.16.11
beautifulsoup4==4.12.3
markdown-it-py==3.0.0

# --- транскрибация на GPU ---
faster-whisper==1.1.0

# --- утилиты ---
tqdm==4.67.1
tenacity==9.0.0
